[pytest]
testpaths = tests
addopts = -s
asyncio_default_fixture_loop_scope = function
markers =
    live: requires explicitly enabled real OAuth, Vertex, Firestore, OneBot, TTS, or other network services
