# 小柠：可自托管的 QQ AI 伙伴

小柠是一套运行在 Windows 上的 AstrBot QQ 助手。它把聊天、公开网页阅读、搜索、翻译、图片和可选的文件/网页/视频能力接到同一个自然语言入口，适合个人或小范围自托管。

## 能力标签

`#小柠` `#QQBot` `#AstrBot` `#自托管` `#Windows` `#LLM` `#OpenAI兼容API` `#NapCat` `#隐私优先`

## 直接这样说

- “帮我查一下今天的 AI 新闻，并列出来源。”
- “把这段话翻译成英文。”
- “帮我画一只雨夜霓虹下的黑猫。”
- “总结一下这个公开链接。”
- “帮我做一份暑假计划 Word。”

## 安装与配置

在公开可读的 GitHub 仓库页面执行 README 中的一串 PowerShell 命令即可安装。首次运行默认使用用户自己的 Google Gemini OpenAI 兼容入口，也可填写其他兼容 API；API Key 不作为命令行参数，不提交到 Git，也不写入日志。

QQ/NapCat 登录需要用户在自己的机器上手动完成。语音、图片生成、视频、网页发布、Firestore 和邮件报告均是可选项，未配置时不会伪装成已完成。

## 隐私边界

- 默认只绑定本机回环地址，API 请求由用户自己的服务商承担。
- 仓库不包含固定的项目号、QQ 号、邮箱、用户路径或访问令牌。
- 普通用户帮助只展示公开能力，不展示管理员命令、白名单或内部实现。
- 文件、图片、网页和语音只有在实际交付后才会报告为完成。

更完整的启动步骤和环境变量见 [README.md](README.md)。
